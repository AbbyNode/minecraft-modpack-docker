# ATM 10
MODPACK_URL=https://mediafilez.forgecdn.net/files/7121/795/ServerFiles-4.14.zip
STARTSCRIPT=startserver.sh

# Backup Configuration
# Backup schedule (daily at 2 AM by default, configured in ofelia/config.ini)
# Retention: 7 daily, 4 weekly, 6 monthly (configured in data/config/borgmatic/config.yaml)

# Chunk Cleanup Configuration  
# Cleanup schedule (weekly on Sunday at 3 AM by default, configured in ofelia/config.ini)
# Cleanup rules (configured in data/config/mcaselector-options.yaml)
