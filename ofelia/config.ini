# Ofelia job configuration
# This file orchestrates scheduled tasks for the Minecraft server infrastructure

# Borgmatic Backup Job
# Runs daily backups at 2:00 AM
[job-exec "borgmatic-backup"]
schedule = 0 2 * * *
container = borgmatic
command = /scripts/backup.sh
no-overlap = true

# MCASelector Chunk Deletion Job
# Runs weekly on Sunday at 3:00 AM to clean up old chunks
[job-exec "mcaselector-cleanup"]
schedule = 0 3 * * 0
container = mcaselector
command = /scripts/delete-chunks.sh
no-overlap = true

# Unmined Map Generation Job
# Uncomment and customize schedule to enable automatic map generation
# Default: Daily at 4:00 AM
# Note: Map generation can take a long time for large worlds
# [job-exec "unmined-render"]
# schedule = 0 4 * * *
# container = unmined
# command = /scripts/render-map.sh
# no-overlap = true
